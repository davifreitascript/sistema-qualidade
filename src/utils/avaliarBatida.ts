/* export function avaliarBatida(valor: string, referencia: number) {
  const numero = Number(valor);

  if (!valor) return null;

  if (numero < referencia) {
    return {
      texto: "Baixo",
      classe: "text-red-600",
    };
  }

  if (numero > referencia) {
    return {
      texto: "Alto",
      classe: "text-red-600",
    };
  }

  return {
    texto: "Bom",
    classe: "text-green-600",
  };
} */